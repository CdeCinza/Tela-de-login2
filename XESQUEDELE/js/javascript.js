//recebendo a resposta do prompt, e salvando na resposta da variavel 
    var resposta = prompt ("qual é a senha ?")

    //Verifica se a resposta é diferente (!=) de "132", se for diferente repete a rotina
    while (resposta != "132"){
        //abre a tela interativa para poder colocar a senha novamente, ele está salvando o valor que ele recebe do prompt, na variavel resposta
    resposta =  prompt ("qual é a senha ?")
    }